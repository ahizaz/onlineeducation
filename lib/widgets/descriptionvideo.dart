import 'package:flutter/material.dart';
class Descriptionvideo extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.only(top: 20),
      child:Column(
        children: [
          Text("App development courses typically entail learning Java programming, Python, or other object-oriented programming languages in a development environment, which is a workspace where programmers can write and make changes to code without permanently altering the finished app.")
        ],
      ),
      
    );
  }
}
